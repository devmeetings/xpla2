import deck from './deck';
import editors from './editors';
import previews from './previews';
import runServerUrl from './runServerUrl';
import workMode from './workMode';

export default {
  deck, editors, previews, runServerUrl, workMode
};
